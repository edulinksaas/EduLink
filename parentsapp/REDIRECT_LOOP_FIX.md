# 무한 리디렉션 루프 해결 가이드

## 🔴 문제 상황

카카오 로그인 시 "리디렉션한 횟수가 너무 많다"는 에러가 발생합니다.

**원인**: 무한 리디렉션 루프가 발생하고 있습니다.

## ✅ 해결 방법

### 1단계: 코드 수정 (완료)

코드에서 `skipBrowserRedirect: true`로 변경했습니다. 이것은 React Native에서 무한 리디렉션을 방지하는 올바른 설정입니다.

### 2단계: Supabase Redirect URLs 설정 확인

**중요**: Supabase의 Redirect URLs에 앱의 deep link가 추가되어 있어야 합니다!

1. **Supabase 대시보드** > **Authentication** > **URL Configuration** 메뉴로 이동

2. **Redirect URLs 섹션 확인**
   - 다음 두 개의 URL이 각각 별도로 추가되어 있어야 합니다:
     1. `https://munydihxxzojgquumdyt.supabase.co/auth/v1/callback`
     2. `parentapp://auth/callback`
   - 각 URL 앞뒤에 공백이 없는지 확인

3. **`parentapp://auth/callback`이 없으면 추가**
   - URL 입력 필드에 `parentapp://auth/callback` 입력 (공백 없이)
   - "Add URL" 버튼 클릭

4. **저장 확인**

### 3단계: 앱 재시작

**중요**: 코드를 수정했으므로 반드시 앱을 재시작해야 합니다!

```bash
cd app
# 개발 서버 완전히 종료 (Ctrl+C)
npx expo start -c
```

앱을 완전히 종료한 후 재시작하세요.

### 4단계: 테스트

1. **카카오 로그인 버튼 클릭**
2. **카카오 로그인 페이지에서 로그인**
3. **리디렉션 루프가 발생하지 않는지 확인**

## 🔍 확인 사항

### Supabase 설정:
- [ ] Redirect URLs에 `parentapp://auth/callback` 추가됨
- [ ] Redirect URLs에 `https://munydihxxzojgquumdyt.supabase.co/auth/v1/callback` 추가됨
- [ ] 두 URL이 각각 별도로 표시됨
- [ ] 각 URL 앞뒤에 공백 없음

### 코드 설정:
- [ ] `skipBrowserRedirect: true`로 설정됨 (완료)
- [ ] `redirectUrl = Linking.createURL('auth/callback')` 사용 중

### 앱 설정:
- [ ] `app.json`에 `"scheme": "parentapp"` 설정됨 (완료)

## ⚠️ 주의사항

1. **`skipBrowserRedirect: true`의 의미**
   - React Native에서는 브라우저 리디렉션을 건너뛰고 직접 처리합니다
   - 이것이 무한 리디렉션을 방지하는 올바른 방법입니다

2. **Deep Link 설정**
   - `parentapp://auth/callback`은 앱의 deep link입니다
   - Supabase Redirect URLs에 반드시 추가되어 있어야 합니다

3. **앱 재시작**
   - 코드를 수정했으므로 반드시 앱을 재시작해야 합니다

## 🆘 여전히 안 되는 경우

### 옵션 1: Deep Link 리스너 확인

앱에서 deep link를 처리하는 리스너가 제대로 설정되어 있는지 확인하세요.

### 옵션 2: Supabase 로그 확인

Supabase 대시보드 > Logs > Auth Logs에서 리디렉션 관련 에러를 확인하세요.

### 옵션 3: 다른 Redirect URL 시도

만약 여전히 문제가 발생한다면, 다른 형식의 redirect URL을 시도해볼 수 있습니다:
- `exp://localhost:8081/--/auth/callback` (Expo 개발 환경용)

## 요약

**문제**: 무한 리디렉션 루프

**해결**:
1. ✅ 코드 수정: `skipBrowserRedirect: true` (완료)
2. ✅ Supabase Redirect URLs에 `parentapp://auth/callback` 추가 확인
3. ✅ 앱 재시작

**핵심**: React Native에서는 `skipBrowserRedirect: true`를 사용하고, Supabase Redirect URLs에 앱의 deep link를 추가해야 합니다!
