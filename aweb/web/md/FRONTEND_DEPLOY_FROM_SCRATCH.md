# 프론트엔드 배포 가이드 (처음부터 다시)

이 가이드는 Vercel을 사용하여 프론트엔드를 처음부터 배포하는 방법을 단계별로 설명합니다.

---

## 📋 사전 준비사항

### 1. GitHub 저장소 확인
- 프론트엔드 코드가 GitHub에 푸시되어 있어야 합니다
- 저장소 이름: `edulinksaas/edulinksaas` (또는 본인의 저장소)
- 프론트엔드 코드 경로: `saas/` (루트에 `src/`, `package.json`, `vite.config.js` 등이 있음)

### 2. 백엔드 URL 준비
- 백엔드가 이미 배포되어 있어야 합니다
- 백엔드 URL 예시: `https://edulinksaas-backend-1.onrender.com`
- 백엔드 API 엔드포인트: `https://edulinksaas-backend-1.onrender.com/api`

**백엔드 URL 확인 방법:**
1. Render Dashboard 접속
2. 백엔드 서비스 선택
3. 서비스 URL 복사 (예: `https://edulinksaas-backend-1.onrender.com`)
4. API 엔드포인트는 `/api`를 포함: `https://edulinksaas-backend-1.onrender.com/api`

---

## 🚀 1단계: Vercel 계정 생성

1. https://vercel.com 접속
2. **"Sign Up"** 또는 **"Get Started"** 클릭
3. **GitHub로 로그인** 선택
4. GitHub 권한 승인
5. 무료 계정 생성 완료

---

## 🛠️ 2단계: GitHub 저장소 연결

1. Vercel Dashboard에서 **"Add New..."** 버튼 클릭
2. **"Project"** 선택
3. GitHub 저장소 연결:
   - **"Import Git Repository"** 클릭
   - GitHub 권한 승인 (처음인 경우)
   - 저장소 선택: `edulinksaas/edulinksaas` (또는 본인의 저장소)
   - **"Import"** 클릭

---

## ⚙️ 3단계: 프로젝트 설정

### 기본 설정
- **Project Name**: `edulink-frontend` (원하는 이름으로 변경 가능)
- **Framework Preset**: **Vite** (자동 감지됨)
- **Root Directory**: `saas` (프론트엔드 코드가 있는 폴더)
  - ⚠️ **주의**: 저장소 루트에 `saas/` 폴더가 있다면 `saas`로 설정
  - 저장소 루트가 프론트엔드라면 `.` (루트)로 설정

### 빌드 설정 (자동 감지됨)
- **Build Command**: `npm run build` (자동 설정됨)
- **Output Directory**: `dist` (자동 설정됨)
- **Install Command**: `npm install` (자동 설정됨)

### 환경 변수 설정
**"Environment Variables"** 섹션에서 다음 변수들을 추가하세요:

#### 필수 환경 변수
```
VITE_API_BASE_URL=https://edulinksaas-backend-1.onrender.com/api
```

#### 환경 변수 설명
- **VITE_API_BASE_URL**: 백엔드 API 엔드포인트 URL
  - 예: `https://edulinksaas-backend-1.onrender.com/api`
  - ⚠️ **주의**: `/api`를 포함해야 합니다
  - 백엔드가 Render에 배포되어 있다면 `.onrender.com` 도메인 사용

#### 환경 변수 추가 방법
1. **"Environment Variables"** 섹션으로 스크롤
2. **"Add"** 또는 **"Add Environment Variable"** 클릭
3. Key: `VITE_API_BASE_URL`
4. Value: 백엔드 API URL 입력 (예: `https://edulinksaas-backend-1.onrender.com/api`)
5. **"Save"** 클릭

---

## 🚀 4단계: 배포 시작

1. 모든 설정이 완료되었는지 확인:
   - ✅ Root Directory: `saas` (또는 `.`)
   - ✅ Framework Preset: `Vite`
   - ✅ Build Command: `npm run build` (자동)
   - ✅ Output Directory: `dist` (자동)
   - ✅ 환경 변수 추가됨: `VITE_API_BASE_URL`

2. **"Deploy"** 버튼 클릭

3. 배포 진행 상황 확인:
   - 배포 로그가 실시간으로 표시됩니다
   - 빌드 과정 확인 가능
   - 배포 완료까지 약 2-5분 소요

---

## ✅ 5단계: 배포 확인

### 1. 배포 완료 확인
배포가 완료되면 자동으로 도메인이 생성됩니다:
- 예: `edulink-frontend.vercel.app`
- 또는 커스텀 도메인 설정 가능

### 2. 프론트엔드 접속 테스트
브라우저에서 다음 URL로 접속:
```
https://your-app-name.vercel.app
```

**예상 결과:**
- 로그인 페이지가 표시됩니다
- 페이지가 정상적으로 로드됩니다

### 3. 백엔드 연결 확인
1. 브라우저 개발자 도구 열기 (F12)
2. **Console** 탭 확인
3. 다음 메시지 확인:
   ```
   🌐 API Base URL: https://your-backend.onrender.com/api
   ```

### 4. 로그인 테스트
1. 로그인 페이지에서 학원 코드와 비밀번호 입력
2. 로그인 시도
3. 정상적으로 로그인되는지 확인

---

## 🔍 문제 해결

### 배포 실패 시

#### 1. 로그 확인
- Vercel Dashboard > 프로젝트 선택 > **"Deployments"** 탭
- 실패한 배포 클릭 > **"Build Logs"** 확인
- 에러 메시지 확인

#### 2. 일반적인 문제들

**문제: "Cannot find module"**
- **원인**: `package.json`의 dependencies가 설치되지 않음
- **해결**: Root Directory가 올바른지 확인 (`saas` 또는 `.`)

**문제: "Build failed"**
- **원인**: 빌드 과정에서 오류 발생
- **해결**: 
  - 로컬에서 `npm run build` 실행하여 오류 확인
  - 빌드 로그에서 구체적인 오류 메시지 확인

**문제: "API 요청 실패" 또는 "CORS 오류"**
- **원인**: 백엔드 URL이 잘못 설정되었거나 CORS 설정 문제
- **해결**: 
  - `VITE_API_BASE_URL` 환경 변수가 올바른지 확인
  - 백엔드 CORS 설정 확인 (백엔드에서 Vercel 도메인 허용)
  - 백엔드 URL에 `/api`가 포함되어 있는지 확인

**문제: "Root Directory not found"**
- **원인**: Root Directory 경로가 잘못됨
- **해결**: 
  - GitHub 저장소에서 실제 경로 확인
  - 저장소 루트 기준 상대 경로로 설정
  - 예: `saas` 또는 `.`

**문제: "환경 변수가 적용되지 않음"**
- **원인**: 환경 변수 이름 오타 또는 재배포 필요
- **해결**: 
  - 환경 변수 이름이 `VITE_API_BASE_URL`인지 확인 (대소문자 구분)
  - 환경 변수 수정 후 재배포 필요
  - Vite는 `VITE_` 접두사가 필요함

#### 3. 환경 변수 재설정
- 프로젝트 선택 > **"Settings"** > **"Environment Variables"**
- 환경 변수 수정 후 **"Save"**
- **"Deployments"** 탭에서 **"Redeploy"** 클릭

#### 4. 수동 재배포
- 프로젝트 선택 > **"Deployments"** 탭
- 최신 배포 선택 > **"Redeploy"** 클릭

---

## 📝 배포 후 체크리스트

- [ ] Vercel 계정 생성 완료
- [ ] GitHub 저장소 연결 완료
- [ ] 프로젝트 생성 완료
- [ ] Root Directory 설정 (`saas` 또는 `.`)
- [ ] Framework Preset 설정 (`Vite`)
- [ ] Build Command 확인 (`npm run build`)
- [ ] Output Directory 확인 (`dist`)
- [ ] 환경 변수 추가 완료:
  - [ ] `VITE_API_BASE_URL` (백엔드 API URL)
- [ ] 배포 완료
- [ ] 프론트엔드 URL 접속 테스트 성공
- [ ] 브라우저 콘솔에서 API Base URL 확인
- [ ] 로그인 기능 테스트 성공

---

## 🔗 백엔드 CORS 설정 확인

프론트엔드가 백엔드에 요청을 보낼 수 있도록 백엔드에서 CORS 설정이 필요합니다.

### 백엔드 CORS 설정 예시 (Express)
```javascript
const cors = require('cors');

app.use(cors({
  origin: [
    'http://localhost:5173', // 로컬 개발
    'https://your-app-name.vercel.app', // Vercel 배포 URL
  ],
  credentials: true,
}));
```

### 백엔드 CORS 설정 확인
1. 백엔드 코드에서 CORS 설정 확인
2. Vercel 도메인을 허용 목록에 추가
3. 백엔드 재배포

---

## 💡 참고사항

### Vercel 무료 티어 제한사항
- **빌드 시간**: 월 6000분 (충분함)
- **대역폭**: 월 100GB (충분함)
- **커스텀 도메인**: 무제한
- **HTTPS**: 자동 적용

### 환경 변수 보안
- 환경 변수는 Vercel 대시보드에서만 관리됩니다
- 코드에 직접 작성하지 마세요
- `.env` 파일은 로컬 개발용입니다
- Vite는 `VITE_` 접두사가 있는 환경 변수만 클라이언트에 노출됩니다

### 빌드 최적화
- Vite는 자동으로 코드 스플리팅과 최적화를 수행합니다
- `vercel.json` 파일이 있으면 추가 설정이 적용됩니다
- 현재 프로젝트에는 SPA 라우팅을 위한 rewrites 설정이 포함되어 있습니다

### 환경 변수 사용법
프론트엔드 코드에서 환경 변수 사용:
```javascript
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:3000/api';
```

---

## 📞 추가 도움말

문제가 계속되면:
1. Vercel 공식 문서: https://vercel.com/docs
2. Vercel 커뮤니티: https://github.com/vercel/vercel/discussions
3. 프로젝트 로그 확인: Vercel Dashboard > Deployments > Build Logs
4. 백엔드 로그 확인: Render Dashboard > Logs 탭

---

## 🎉 배포 완료 후

프론트엔드 배포가 완료되면:

1. **프론트엔드 URL 복사**: `https://your-app-name.vercel.app`
2. **백엔드 CORS 설정 확인**: 백엔드에서 Vercel 도메인 허용
3. **전체 기능 테스트**: 로그인, 데이터 조회, CRUD 작업 등
4. **커스텀 도메인 설정** (선택사항): Vercel에서 도메인 연결 가능

---

## 📋 빠른 참조

### 필수 환경 변수
```
VITE_API_BASE_URL=https://edulinksaas-backend-1.onrender.com/api
```

### 주요 설정
- **Root Directory**: `saas` (또는 `.`)
- **Framework**: `Vite`
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

### 배포 URL 형식
- Vercel: `https://your-app-name.vercel.app`
- Render (백엔드): `https://your-backend.onrender.com`
