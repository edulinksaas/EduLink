# Vercel 프로젝트 생성 상세 가이드

이 문서는 EduLink 프론트엔드를 Vercel에 배포하기 위한 프로젝트 생성 과정을 단계별로 설명합니다.

## 📋 사전 준비

- ✅ GitHub 저장소가 생성되어 있음: `edulinksaas/EduLink`
- ✅ Vercel 계정이 있음
- ✅ GitHub 계정이 Vercel에 연결되어 있음

---

## 🚀 단계별 프로젝트 생성

### 1단계: 프로젝트 생성 시작

1. Vercel 대시보드 접속: https://vercel.com/dashboard
2. 상단의 **"Add New..."** 또는 **"+ New Project"** 버튼 클릭
3. 또는 왼쪽 사이드바에서 **"Add New..."** > **"Project"** 클릭

### 2단계: GitHub 저장소 선택

1. **"Import Git Repository"** 섹션에서 GitHub 아이콘 클릭
2. GitHub 저장소 목록에서 **`edulinksaas/EduLink`** 선택
3. 저장소가 보이지 않으면:
   - **"Adjust GitHub App Permissions"** 클릭
   - 저장소 접근 권한 확인
   - 또는 **"Configure GitHub App"** 클릭하여 권한 설정
4. 저장소 선택 후 **"Import"** 버튼 클릭

### 3단계: 프로젝트 설정

프로젝트 설정 화면에서 다음 항목들을 설정합니다:

#### 기본 설정

- **Project Name**: 
  - 기본값: `EduLink` 또는 원하는 이름
  - 예: `edulink-frontend`, `edulink-saas`

- **Team**: 
  - `edulink's projects` 선택 (또는 개인 계정)

#### 빌드 설정 (중요!)

- **Framework Preset**: 
  - **Vite** 선택 (자동 감지될 수도 있음)

- **Root Directory**: 
  - **`saas`** 로 변경 ⚠️ (중요!)
  - 기본값은 `.` (루트)인데, 프론트엔드 코드가 `saas` 폴더에 있으므로 변경 필요

- **Build Command**: 
  - 기본값: `npm run build` (그대로 사용)
  - 또는 `cd saas && npm run build` (Root Directory를 변경했다면 `npm run build`만)

- **Output Directory**: 
  - 기본값: `dist` (그대로 사용)
  - Root Directory를 `saas`로 설정했다면 `dist`만 입력

- **Install Command**: 
  - 기본값: `npm install` (그대로 사용)

#### 환경 변수 설정 (선택사항)

이 단계에서 환경 변수를 추가할 수도 있지만, 나중에 추가해도 됩니다:

- **VITE_API_URL**: 백엔드 API URL
  - 예: `https://your-backend.railway.app`
  - 나중에 Settings에서 추가 가능

### 4단계: 배포 실행

1. 모든 설정 확인 후 **"Deploy"** 버튼 클릭
2. 빌드 과정이 시작됩니다 (1-3분 소요)
3. 빌드 완료 후 배포 URL이 생성됩니다
   - 예: `https://edulink-frontend.vercel.app`

### 5단계: Project ID 확인

프로젝트가 생성되고 배포가 완료된 후:

1. 프로젝트 대시보드에서 **"Settings"** 탭 클릭
2. 왼쪽 메뉴에서 **"General"** 선택
3. **"Project ID"** 섹션에서 ID 확인
   - 형식: `prj_xxxxxxxxxxxxxxxxxxxxx`
4. 이 ID를 복사하여 GitHub Secrets에 추가합니다

---

## ✅ 설정 확인 체크리스트

프로젝트 생성 후 다음을 확인하세요:

- [ ] Root Directory가 `saas`로 설정되어 있는가?
- [ ] Framework Preset이 Vite로 설정되어 있는가?
- [ ] Build Command가 `npm run build`인가?
- [ ] Output Directory가 `dist`인가?
- [ ] 배포가 성공적으로 완료되었는가?
- [ ] Project ID를 확인했는가?

---

## 🔧 추가 설정 (배포 후)

### 환경 변수 추가

1. 프로젝트 Settings > **Environment Variables** 탭
2. **Add New** 클릭
3. 다음 변수 추가:
   - **Key**: `VITE_API_URL`
   - **Value**: 백엔드 API URL
   - **Environment**: Production, Preview, Development 모두 선택
4. **Save** 클릭

### 도메인 설정 (선택사항)

1. Settings > **Domains** 탭
2. 원하는 도메인 추가
3. DNS 설정 안내에 따라 도메인 연결

---

## 🐛 문제 해결

### 빌드 실패 시

1. **Root Directory 확인**
   - `saas` 폴더가 올바르게 설정되었는지 확인
   
2. **Build Log 확인**
   - 배포 페이지에서 **"View Function Logs"** 클릭
   - 오류 메시지 확인

3. **로컬 빌드 테스트**
   ```bash
   cd saas
   npm install
   npm run build
   ```
   - 로컬에서 빌드가 성공하는지 확인

### 저장소가 보이지 않을 때

1. Vercel Settings > **Git** 탭
2. GitHub 연결 확인
3. 필요시 **"Configure GitHub App"** 클릭하여 권한 재설정

### Root Directory를 잘못 설정했을 때

1. 프로젝트 Settings > **General** 탭
2. **Root Directory** 수정
3. **Save** 클릭
4. 새 배포 트리거 (코드 푸시 또는 수동 재배포)

---

## 📝 다음 단계

프로젝트 생성이 완료되면:

1. ✅ Project ID를 GitHub Secrets에 추가
2. ✅ Organization ID 확인 및 추가
3. ✅ API Token 생성 및 추가
4. ✅ 환경 변수 설정
5. ✅ GitHub Actions 자동 배포 테스트

자세한 내용은 `SECRETS_SETUP_GUIDE.md`를 참고하세요.

---

## 🔗 참고 링크

- [Vercel 프로젝트 생성 가이드](https://vercel.com/docs/concepts/deployments/overview)
- [Vercel 환경 변수 설정](https://vercel.com/docs/concepts/projects/environment-variables)
- [Vite 프로젝트 배포](https://vercel.com/docs/frameworks/vite)

